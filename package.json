{
  "name": "marketplace",
  "version": "0.1.0",
  "private": true,
  "description": "OURO (ouro.lifestyle) — an NFT marketplace for every KCS-2 collection on Koinos. KOIN prices, 2.5% platform fee, royalties honored, mana sponsored.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "echo \"no build step — OURO serves static files and server.js directly\"",
    "test": "node tests/market-check.js && node tests/cover-check.js && node tests/recover-check.js",
    "test:offline": "node tests/cover-check.js && node tests/recover-check.js"
  },
  "dependencies": {
    "jimp": "^1.6.1",
    "koilib": "^9.1.0"
  }
}
